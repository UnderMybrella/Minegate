package org.abimon.mods.minecraft.minegate;

import net.minecraft.world.World;

/** Just for reference at the moment */
public interface INaquadahBlock {
	/** Should this block do stuff? */
	//public boolean isNaquadahBlock(World world, int x, int y, int z);
}
